/**
 * Returns a string greeting the provided entity.
 * @param entity - The entity to greet.
 * @returns - Greeting.
 */
export function hello(entity: string): string {
    return `Hello, ${entity}!`;
}
